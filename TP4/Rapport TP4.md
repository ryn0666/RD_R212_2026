Rapport TP4

Points clés

1. fetch & async/await
J’ai compris que fetch est asynchrone : await attend la réponse avant de continuer, et async est obligatoire devant la fonction qui l’utilise.

2. Les 3 états
Pour toute donnée distante, il faut toujours gérer loading, success et error avec un try/catch.

3. Explorer avant d’afficher
Avec REST Countries, j’ai vu qu’il faut toujours faire un console.log pour comprendre la structure des données d’une API avant de les afficher.

Utilisation de l’IA
L’IA m’a aidé à repérer que j’oubliais d’appeler afficherRecherche(pays) dans le Temps 3.​​​​​​​​​​​​​​​​